[pytest]
addopts = -ra
markers =
    smoke: Fast high-confidence checks for basic app health.
    regression: Core suite coverage for normal release validation.
    slow: Longer-running tests (search, history, notifications, uploads).
    destructive: Tests that may create/update/delete data in the target environment.
